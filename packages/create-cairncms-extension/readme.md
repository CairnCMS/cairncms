# create-cairncms-extension

A small util that will scaffold a CairnCMS extension.

## Installation

This package is meant to be used through `npm` or `yarn`:

```
npm init cairncms-extension
```

```
yarn create cairncms-extension
```
